# TsumeTsume_Strategy

ファイアーエムブレムif暗夜をモデルにした 2D タクティクスSRPG。Unity 6 (6000.3.10f1) + URP。
作者はプログラミングほぼ初心者。日本語で、専門用語をかみ砕いて丁寧に説明すること。絵文字は使わない。

## 進捗状況（2026-07-19 時点）

バーティカルスライス完成（Phase 0〜7）、Phase 8（データ基盤刷新）、
Phase 9（前衛・後衛武器と新三すくみ、CombatRules による判定一元化）、
Phase 10（コマンドメニューUI・移動取り消し）、
Phase 11（救出システム）、Phase 12（輸送隊・輸送隊死亡で敗北）、
Phase 13（地形システム）、Phase 14（空中移動・飛翔、城壁地形、着陸コマンド）、
Phase 15（統合仕上げ: ターン制限・可変盤面・騎乗地形制限・戦闘予測・Stage_01）、
Phase 16（敵AI土台: 経路ベースの接近・評価/実行の分離・思考ログ・検証ステージ）、
Phase 17（敵の性格付け: 突撃型/待ち伏せ型・挑発システム）、
Phase 18（挟撃の活用: お膳立て加点・ガード役潰し・挟撃判定の部品化）、
Phase 19（ガードの活用: 挟撃されそうな味方への守り配置・ThreatMap 新設）、
Phase 20（飛翔の活用: 敵飛行兵の離陸・巡航・着陸判断・probe方式）、
Phase 21（集中攻撃: 敵フェイズ開始に「皆で倒し切れる相手」を1体選んで攻撃を収束）まで
Play 確認済み・コミット済み。**Phase 8〜15 ロードマップと敵AI強化ロードマップ（Phase 16〜21）は完走**。

その後 2026-07-18 に、デバッグ/テスト用UI 4件（作者依頼。「デバッグ/テスト用UI」の節参照）と
能力値調整（歩兵系ユニットの移動力 4→3）を実施。ロードマップ最終項目の統合調整
（Stage_01 の通しプレイ確認）は、作者のプレイの結果「手応え・面白さ十分、調整不要」で完了
（点数定数・アセットの追加変更なし）。

さらに同日（2026-07-18）、**兵種（クラス）システムのデータ基盤**を導入:
ClassData（ScriptableObject）38種（下級14職・上級24職。「兵種データの合意仕様」の節参照）を新設し、
全ユニット17体を兵種に紐付け（移動タイプ・移動力は兵種データ由来へ）。武器種に杖・光魔法を追加（定義のみ）。

兵種データ基盤は 2026-07-19 に作者が Play 確認し、コミット済み（feat 59de805 / data 5174bf1 / docs 96f0691）。
続けて**所持品（インベントリ）ロードマップ（Phase 22〜25）を完走**（2026-07-19 キックオフ〜完了）:
フェーズ22（データ基盤・挙動不変 3293343）→23（装備コマンド feat d8f5d04/data cb6effb）→
24（道具・傷薬 feat f689ddf/data 144a598）→25（杖「ライブ」feat 00a1411/data 1f35a2a）まで
すべて Play 確認済み・コミット済み。所持品は特効武器・武器耐久・交換・杖効果・ショップの土台で、
兵種の「装備可能武器」データが修道士の杖でようやく本来の役割を果たした
（詳細は「所持品の合意仕様」「杖（ライブ）の合意仕様」の節）。

過去のロードマップ3本（いずれも完走済み。経緯・キックオフ合意の参照用）:
Phase 8〜15 は `/home/wawo/.claude/plans/tsumetsume-strategy-phase8-tsumetsume-st-elegant-parasol.md`、
敵AI強化（Phase 16〜21）は `/home/wawo/.claude/plans/tsumetsume-strategy-ai-ai-ai-robust-quasar.md`、
所持品（Phase 22〜25）は `/home/wawo/.claude/plans/tsumetsume-strategy-glimmering-horizon.md`
（いずれもWSL側）。注意: 旧ロードマップの Phase 14 節は初版案のまま。飛翔の確定仕様は
本ファイルの「飛翔の合意仕様」の節が正（2ターン→3ターン等、作者改訂済み）。
ゲーム根幹の仕様は `docs/コアシステム仕様書.md`（リポジトリ側を正とする。Desktop の原本は作者の編集用）。

## 確定済みの仕様（作者との合意。勝手に変えない）

- 命中は必中（命中・回避RNGなし）。ダメージ = max(0, 攻撃+武器威力+三すくみ補正-(防御+地形防御))
- 武器分類（WeaponCategory）: 剣・斧・槍＝前衛(Melee=0)、弓・魔導書・光魔法・杖＝後衛(Ranged=1)。データ駆動（WeaponData.category）。
  武器種（WeaponType）は7種: 剣・槍・斧・弓・魔導書（＝魔法。同一武器）・杖・光魔法（2026-07-18 追加。光魔法は定義のみ）。
  魔法ダメージ（魔力vs魔防）の判定は WeaponType.IsMagicDamage()（魔導書・光魔法）。
  杖は攻撃できない後衛武器＝回復に使う（前衛から攻撃されると武装無し扱い。Phase 25「杖（ライブ）の合意仕様」の節）
- 三すくみ（Phase 9 で新式へ移行済み）: 前衛武器の攻撃のみ。剣>斧>槍>剣。
  有利側が攻撃 = +max(2, 攻撃側の技−防御側の速さ)、有利側が防御 = −max(2, 防御側の速さ−攻撃側の技)。
  後衛武器・武装無しは前衛から攻撃されると「武装無し扱い」で不利判定。後衛側の攻撃時は三すくみなし
- 後衛武器の射程: 移動後は最小射程（2マス）ちょうど、静止時（自マスクリック）は 2〜武器上限（弓3・魔道書2）
- 反撃は基本なし。CombatSystem.CanCounter() フックが唯一の拡張点（現状常に false）
- 挟撃: 近接攻撃時、敵を挟んだ反対側の隣マス(上下左右)に前衛武器の味方がいれば追加攻撃。
  参加した味方は行動を消費しない。遠距離・後衛武器の味方では不成立。
  ガード（無効化）: 防御側の隣接4マスに同陣営の「前衛武器の歩兵」がいると挟撃不成立。
  ガード役自身は守られない。行動済みでも有効。敵味方対称
- 戦闘の可否・補正判定は CombatRules に一元化。UIとAIは必ず同じ関数を呼ぶ。
  合計ダメージ予測は CombatSystem.PredictTotalDamage（挟撃・ガード込み。AI評価と将来の予測UIの共有窓口）
- 武装無しユニットには攻撃コマンドを出さない（CanAttack が false）
- データは ScriptableObject 中心（UnitData / WeaponData）。数値調整は作者がエディタで行う
- 座標は左下(0,0)、X右+、Y上+。グリッドは見た目(コード生成の四角)と論理データ(TileData[,])の二層
- 仮素材（色付き四角＋IMGUI）で進行中。本番素材への差し替えは後
- 能力値は7種（HP・力・魔力・技・速さ・守備・魔防）。物理=力vs守備、魔法=魔力vs魔防（地形防御は両方に加算）
- 兵種の階層: 歩兵／騎乗兵（騎兵・飛行兵）。輸送隊は騎兵の一種（2026-07-11 合意）。
  騎兵向けルールは UnitClass.IsCavalry()（騎兵+輸送隊）、騎乗全体は IsMounted() を使う。
  兵種データ導入後（2026-07-18）、UnitClass は「移動タイプ」の分類として使われる
- 兵種（クラス）: ClassData（ScriptableObject）38種で定義（「兵種データの合意仕様」の節参照）。
  移動タイプ・移動力・装備可能武器は兵種で決まる。移動力の実数値は
  Assets/_Game/Data/Classes/Class_*.asset が正で、作者が Unity のエディタで調整する
  （旧「標準移動力」の合意は兵種ごとの値に置き換え。例: 剣闘士3・剣士4・重装兵2・騎馬兵6・竜騎兵5）
- Unit は Initialize で UnitData をコピーした「ランタイム能力値」を使う（Strength〜Move/Class/Weapon プロパティ経由。
  移動タイプと移動力は UnitData.EffectiveClass/EffectiveMove＝兵種データ優先・未設定時は旧フィールドで解決。
  Data 直読みは Unit 内の窓口のみ。ユニット列挙は UnitRegistry（名簿）経由で、FindObjectsByType は使わない）
- 移動: 味方のマスは通過可・停止不可、敵のマスは侵入・通過不可。非アクティブ化＝盤上から外れる（占有も明け渡す）。
  飛翔中の例外は「飛翔の合意仕様」の節を参照
- 操作フロー（Phase 10〜）: 移動 → 行動メニュー（攻撃・被ダメ確認・待機。状況に応じて
  救出系・飛翔・着陸も並ぶ）→ 対象選択 のFE型。
  キャンセルは右クリック/ESC で1段階ずつ戻る（対象選択→メニュー→移動取り消し→選択解除）。
  移動の取り消しは ActionContext.RevertMove（占有も復元）。待機はメニューから明示的に選ぶ
  （旧「敵以外クリックで待機」は廃止）。メニューはユニット右隣・画面端で左に回り込み
- マスのハイライトは HighlightKind の優先度付き多層管理
  （Selection > TargetChoice > AttackRange > HealRange > MoveRange > 市松模様）。色設定は GridManager に集約

### 救出システムの合意仕様（Phase 11・2026-07-11 作者合意）

- 救出: 騎乗ユニットのみ。対象は隣接する同陣営の歩兵（騎乗・輸送隊は被救出不可）。
  行動済みの味方も救出できる。救出後は「使っていない移動力分の再移動 → 待機」のみ
- 救出中のペナルティなし。救出中でも攻撃可能。救出と同一行動での「降ろす」は不可
- 降ろす: 隣接する空きマスへ再配置。降ろされたユニットはそのターン行動済み。
  降ろしたら（代わりに降ろすも含め）そのユニットの行動は終了
- 引き受け: 空きのある騎乗ユニットが、隣の救出中の味方から貨物をもらう。
  直後にできるのは「降ろす/待機」のみ（引き受け自体では行動済みにならない）
- 代わりに降ろす: 歩兵専用。貨物が歩兵で「貨物の移動力 ≧ 自分の移動力」のときのみ
- 救出・引き受けを実行したら取り消し不能（選択解除も不可。必ず待機等で行動を終える）
- 運び手が倒されたら、貨物はその場（死亡マス）に降ろされて生存
- 格納中のユニットは盤上から除外（占有明け渡し・敵AIの対象外・GetUnits に出ない）が、
  生存数（CountAlive）には数える → 「救出中の騎兵＋格納歩兵」だけでも敗北しない
- 可否判定は RescueRules に一元化（CombatRules と同じ方針）

### 輸送隊の合意仕様（Phase 12・2026-07-11 作者合意）

- 輸送隊は容量4。騎乗ユニット（騎兵・飛行兵）も救出・引き受けできる（通常の騎乗兵は歩兵のみ）
- 誰も輸送隊を救出できない。輸送隊自身も他へ格納されない（乗り込みも不可）
- 入れ子救出は禁止：貨物を持っているユニットは救出・乗り込みの対象にならない（合意(a)）
- 乗り込む：輸送隊に隣接した歩兵・騎乗兵が自分から格納される。
  自分は行動終了、輸送隊の行動は消費しない
- 貨物が複数のとき、降ろす・引き受け・代わりに降ろすは貨物リスト（CargoListMenu）から
  対象を選ぶ。1体だけなら自動選択でリストは出ない
- 引き受けの種別制限：通常の騎乗兵が受け取れるのは歩兵の貨物のみ。輸送隊は何でも受け取れる
- **味方の輸送隊が倒されたら敗北（ゲームオーバー）**。貨物の有無は問わない。
  FE上級者向けに自由度より機能・挙動を守る方針（作者）。
  通常の騎乗兵の死亡は従来どおり（貨物はその場に降ろされて生存、ゲームは続く）
- 輸送隊は当面プレイヤー専用（敵AIは救出系を使わないため。合意(c)）
- 輸送隊は武器なしユニット（攻撃コマンドが出ない。Phase 9 の武装無し分岐の実地例）

### 地形の合意仕様（Phase 13・2026-07-11 作者合意）

- 地形は5種: 平地`.`（コスト1/防御+0）、森`F`（2/+1）、山`M`（3/+2）、砦`T`（1/+3）、壁`#`（通行不可）。
  数値・色は TerrainTable.asset の編集だけで調整できる（記号は terrainRows で使う1文字）
- 地形コストは当面全兵種共通 →（Phase 15 で兵種別コスト・通行制限を導入。「Phase 15 の合意仕様」の節が正）
- 兵種限定の通行制限（山は歩兵のみ等）は当面なし →（同上。Phase 15 で導入済み）
- 地形防御は物理・魔法の両方に加算（Phase 8 合意の継続。DamageBreakdown の「地形」項）
- ステージの地形は StageData の文字マップ terrainRows（リスト先頭=盤面の最上段の行）。
  行数・文字数の不一致や未知の記号は警告して平地扱い
- 地形の適用は BattleSetup.Start → GridManager.ApplyTerrain の流れ（シーン参照の追加は不要）
- 表示は地形色×市松の明暗（GridManager.checkerDarken）。ハイライトは Color.Lerp で
  地形色に混ぜる（highlightBlend）ので地形とハイライトが同時に判別できる。
  カーソル下マスの地形名/コスト/防御を画面左下に1行表示（IMGUI）
- 移動範囲計算はダイクストラ法（コスト差のある地形で最短経路を保証）。
  マス進入コストは MovementCalculator.GetMoveCost(unit, tile) に分離
  （Phase 14 で「飛翔中は常に1」を差し込む拡張点）

### 飛翔の合意仕様（Phase 14・2026-07-12 作者合意。ロードマップの初版案から作者改訂あり、本節が正）

- 「飛翔」は飛行兵専用コマンドで、**行動を消費しない**。非飛翔中ならいつでも使える（移動前・移動後・救出後も）。
  使用後は残り移動力ぶんの飛行移動が可能。飛翔後に使えるコマンドは救出・降ろす以外（攻撃は飛翔中の敵のみ）
- 持続は**発動ターンを含めて3ターン**（表示「飛x3→飛x2→飛x1」。自軍フェイズ開始ごとに減り、0で自動着地）。
  着地したターンは地上ユニットとして普通に行動できる。着地したターンの再飛翔も可（クールタイムなし）
- 「着陸」コマンド: 飛翔中の飛行兵が使える（飛翔を発動したターンは不可）。飛翔を解除して**即行動済み**。移動→着陸も可
- 飛翔中の移動: 全マス移動コスト1（地形無視）。屋内壁 `#` は飛行でも通過不可。
  城壁 `=`（Phase 14 新地形。地上は通行不可・地形防御+2）は飛行なら通過も停止も可。
  飛翔が切れたとき城壁の上にいたら、そのまま城壁の上に着地する（翌ターン歩いて降りられる。詰みなし）
- すり抜け: 飛翔中は敵のマスを通過できるが、**「飛翔状態の敵」と「対空武器（弓・魔法）装備の敵」のマスは通過不可**。
  地上ユニットは飛翔中の敵の下を通過できる。停止は誰もいないマスのみ（着地衝突は仕組み上起こらない）
- 戦闘: 飛翔中の相手を攻撃できるのは「飛翔中のユニット」か「後衛武器（対空）」のみ。飛翔中は地上の敵を攻撃できない。
  飛翔中は地形防御を受けない。判定は CombatRules.CanEngage に一元化（UI・敵AI・挟撃が共有）
- 挟撃: 飛翔中の敵を挟めるのは飛翔中の前衛味方のみ。地上の前衛歩兵ガードは飛翔中の防御側を守れない
- 救出との関係: 飛翔中は救出系に原則関与できない（実行も対象も不可。乗り込むも不可）。
  例外は「引き受け」で、**飛翔状態が同じ相手同士**（地上同士・空中同士）なら可。
  空中で引き受けた貨物は降ろせない（降ろすは地上でのみ表示）。貨物を抱えたまま飛翔は可（着地後に降ろせる）
- 取り消しは1段階ずつ: メニュー→飛翔後の再移動取り消し（飛翔した位置へ）→飛翔解除→移動取り消し→選択解除。
  行動確定前なら範囲外クリック・別ユニットクリックでも飛翔は取り消される
  （救出後など取り消し不能のときは飛翔の取り消しだけが起こり、選択は残る）。
  飛翔が確定するのは待機・攻撃・着陸などで行動を終えたとき
- 敵AIは飛翔コマンドを使わない（従来合意）。StageData の配置ごとに「開始時飛翔ターン数」を指定でき、
  検証用の敵飛行兵は99ターンで最初から飛んでいる（Stage_Test。数値はアセット編集で変更可）

### Phase 15 の合意仕様（統合仕上げ・2026-07-16 作者合意）

- 勝敗条件はテスト用ではなく本番システム。今回は**ターン制限のみ追加**
  （StageData.turnLimit。0=無制限。最終ターンの敵フェイズ終了時に勝敗未決なら敗北。
  判定は増加前に行うので表示が制限を超えない。制限中は「ターン n／上限」表示）。
  拠点到達などの勝利条件バリエーションは次期ロードマップへ先送り（作者合意）
- 兵種別地形（通行制限まで導入）: 騎乗ユニット（騎兵・輸送隊・地上の飛行兵=IsMounted）は
  森コスト3（歩兵2）、山は**通行不可**（歩兵のみ）。TerrainDef の mountedWalkable / mountedCost
  （0=共通コストと同じ）で地形ごとに調整できる。飛翔中は従来どおり全マスコスト1
- 降ろす・代わりに降ろす・死亡時解放の配置先も**貨物の兵種**で判定
  （騎乗の貨物は山へ降ろせない。TileData.IsWalkableFor / MoveCostFor が判定窓口）
- 盤面サイズはステージごとに可変（StageData.gridWidth / gridHeight。0以下なら GridManager の
  Inspector 値）。カメラは GridManager.FitCamera が自動フィット（余白は cameraMargin）
- 戦闘予測（BattleForecast・IMGUI右下）: 対象選択中に攻撃対象へマウスを重ねると
  ダメージ内訳・挟撃の追加ダメージ（またはガードで無効）・相手の残りHP見込み・撃破を表示。
  実戦と同じ DamageCalculator / CombatRules を使うので数値は必ず一致する
  →（2026-07-18 のUI改良で「対象選択に入った瞬間の即表示＋三すくみ専用行」へ更新。
  「デバッグ/テスト用UI」の節が正）
- 敵AI: 攻撃先・接近先の点数が同点なら地形防御の高いマスを優先（飛翔中は地形防御0扱い）。
  既知の限界: 接近はマンハッタン距離基準のため、城壁の向こうの敵が門へ回り込まないことがある
  （次期の敵AI強化で対応予定）
- 新ユニット5種: 味方斧兵・味方斧騎兵・敵魔道士・敵槍飛行兵・敵斧騎兵（能力値は初期案。作者が調整）
- 正式ステージ Stage_01: 14×12・ターン制限20・味方10体対敵12体。南が自軍、中央に山脈と森、
  北に城壁（門は敵弓兵が対空で防衛）。BattleScene の BattleSetup は Stage_01 を参照中
  （Stage_Test に戻すのも Inspector の差し替えだけ）
- 次期ロードマップの最優先は**敵AIの強化**（作者の指名）

### 敵AIの性格付けの合意仕様（Phase 17・2026-07-17 作者合意）

- 性格は配置ごとに指定（StageData.Placement.aiProfile。敵のみ有効・未記載=突撃型）。
  種類は突撃型（従来どおり）と待ち伏せ型の2つ
- 待ち伏せ型は**挑発されるまで一切動かない**（接近もしない）。ただし攻撃探索は通すので、
  射程圏（移動+射程）に入られたら普通に移動して攻撃する。
  立ち位置による受動的な効果（隣の味方への挟撃ガード）は未挑発でも効く
- 挑発の条件は2つ: 「被弾している（CurrentHP < MaxHP）」または
  「今すぐ攻撃できる相手がいる（攻撃すること自体が挑発）」。
  連鎖挑発（近くの味方の起動で自分も起動）は v1 では無し
- **挑発は永続**（Unit.IsProvoked。戻す手段なし）。挑発後はずっと突撃型として振る舞う
  → 一度起動した敵は、プレイヤーが退いても持ち場に戻らず追ってくる
- Stage_01 は城内4体（門の弓兵2・魔道士・地上の飛行兵(10,9)）が待ち伏せ型。
  飛翔中の飛行兵(3,9)と野戦部隊7体は突撃型のまま
- 「挑発後も動かない完全固定砲台型」が欲しくなったら3つ目の性格として追加可能（先送り）
- 検証用ステージ: Stage_AITest_Ambush（10×10・砦(7,7)に待ち伏せ敵兵・(2,8)に待ち伏せ敵弓兵）

### 挟撃の活用の合意仕様（Phase 18・2026-07-17 作者合意）

- 挟撃を「完成させる」側は従来から最適（PredictTotalDamage が挟撃込みで評価）。
  本フェーズで足したのは「お膳立て」側と「ガード役潰し」の2つの加点
- お膳立て（PincerSetupBonus +2）: 隣接攻撃のマス選びで、反対側マスに
  「未行動・前衛武器・自分以外」の味方が移動で立てるなら加点。
  控えめな +2 なので実ダメージ差1点までしか覆さない（お膳立ては投機的 —
  後続の味方がもっと良い獲物を選ぶかもしれない — ので実利を犠牲にしない。作者合意）
- お膳立ての条件: 隣接攻撃／相手が非ガード状態（IsPincerNegated が false）／
  反対側マスが盤内かつ空き／後続の味方がそこへ到達可能かつ相手と交戦可能（CanEngage）。
  後続の到達マス集合は TakeAction ごとに1回だけ計算（BuildPincerHelpers）。
  待ち伏せ型の未挑発の味方も後続候補に含む（挟みに行く攻撃自体が挑発になるため）
- ガード役潰し（GuardBreakKillBonus +50）: 一撃で倒せる相手が複数いるとき、
  誰かの挟撃ガード役をしている相手（IsGuardingSomeone）を優先して倒す。
  撃破ボーナスが付くときだけ加算（ガードはHPが残る限り有効なので、倒し切れなければ無意味）
- 点数の階層: KillBonus 1000 ＞（Phase 21 予定: 集中攻撃 200）＞ ガード役潰し 50 ＞ お膳立て 2
- 思考ログに「・挟撃のお膳立て」「・ガード役潰し」を表記
- 抽出リファクタ（挙動不変）: CombatRules.GetPincerOppositeCell（反対側マス）と
  WouldGuardAt（仮定位置でのガード判定。Phase 19 の守り配置AIで使う）を新設し、
  FindPincerAlly / FindPincerGuard をその上に再実装
- 検証用ステージ: Stage_AITest_Pincer（10×10・森(3,4)のみ。ダミー(3,3)を敵兵(3,6)が
  平地(2,3)からお膳立て→敵兵(6,5)が(4,3)で挟撃完成。味方兵(1,1)を隣接させるとガードで
  お膳立てが消えて森に戻る。北東のダミー3体+敵兵(8,5)はガード役潰しの確認用 —
  ダミーのHPを一時的に5へ下げると「撃破・ガード役潰し」が観察できる）

### ガードの活用の合意仕様（Phase 19・2026-07-18 作者合意）

- 攻撃優先: ガード役の敵でも攻撃できる相手がいれば必ず攻撃する
  （守り位置を離れて攻撃に行くこともある。思考ログで分かる）
- ガード＞接近: 防げるダメージが1以上なら接近よりガードを優先。
  マスの選び方は 防止量の合計 → 地形防御 → 移動の少なさ の順
  （1マスで複数の味方を守れたら防止量は合算。候補に現在地も含むので「その場で守り続ける」が出る）
- ガード役になれるのは「前衛武器の歩兵」のみ（WouldGuardAt と同条件）。
  未挑発の待ち伏せ型はガード移動しない（性格ゲートが先。立ち位置による受動ガードは効く）
- 脅威の判定は AI/ThreatMap.FindPincerThreatenedAllies(grid, self):
  非飛翔で（self 以外に）未ガードの味方ごとに上下/左右の対マスを調べ、両側それぞれに
  「前衛のプレイヤーが既に立っている or 空きマスで移動して立てる」があり、両側が
  別々のプレイヤーなら脅威。防止量 = 挟撃に参加できるプレイヤーの最大ダメージ（やや過大見積り）
- self（これから動く敵）は盤面に居ない扱いで判定する（ロードマップの案から引数を1つ追加）:
  自分のガードは「ガード済み」に数えない＋自分が塞いでいる対マスは空き扱い
  （相手の到達判定は「隣まで来られれば入れる」の安全側の丸め）。
  これが無いと、守り続けている敵が翌ターン「もう守られている」と勘違いして持ち場を離れる
- 先に動いた敵が守った味方は IsPincerNegated になり自動で脅威から外れる（重複ガード防止）
- ガード役自身が挟撃される可能性は v1 では読まない（許容・ログで分かる。作者合意）
- 検証用ステージ: Stage_AITest_Guard（10×10全平地・味方騎兵(4,0)(6,0)・敵兵(2,6)(2,4)・
  待ち伏せ型の敵魔道士(5,6)。敵兵が(4,6)へ守りに動き「ガード・最大12ダメージ防止」、
  2体目は接近（重複ガードしない）、騎兵を退げると脅威が消えて守りをやめる）

### 飛翔の活用の合意仕様（Phase 20・2026-07-18 作者合意）

- probe方式（お試し飛翔）: AIの仮定評価は、評価のあいだだけ実際に StartFlight / CancelFlight で
  状態を切り替えて測り、try/finally で必ず元に戻す。probe は2箇所だけ
  （ComputeGroundInfo の一時着地＋TryPlanTakeOff のお試し飛翔）。共有コードは無変更。
  Unit.cs に「StartFlight/CancelFlight に副作用を足すと probe が壊れる」警告コメントあり
- 着陸後の攻撃位置は必ず「地上の状態」で計算する（飛翔中は CanEngage が地上の敵に false を
  返すため。Phase 20 唯一の非自明点）
- 離陸条件（地上の飛行兵・攻撃探索が空振りのとき、いずれかで離陸）:
  (1) 離陸すれば今すぐ空対空攻撃できる（飛翔は行動を消費しない＝離陸→移動→攻撃が1行動）
  (2) 地上では目標へ到達できないが空中なら行き先がある
  (3) 空路ターン数+1 ≤ 陸路ターン数（ターン数=道のり÷移動力の切り上げ。+1は着陸行動の分。
      同数なら空路優先＝門を味方に譲れて移動中に近接に絡まれない）
  (4) 空中でしか攻撃できない相手（飛翔中のプレイヤー）がいれば、空路が速くなくても離陸して追う
  未挑発の待ち伏せ型は離陸しない（性格ゲートが先。Phase 17 合意の延長）
- 空中巡航: 対空射程マス（ThreatMap.GetAirThreatCells＝後衛武器/飛翔中のプレイヤーの
  移動×射程。移動後の最小射程ルールも反映）を「1マス分の遠回りまでなら」避けて、
  目標への空路が一番縮むマスへ（AirThreatPenalty 1）。
  ログに「・対空圏を回避」「・対空圏内（回避先なし）」を表記
- 着陸判断: 残り飛翔2ターン以上で「着陸マスから次のターンに攻撃位置へ届く（地上の道のり ≤ 移動力）」
  なら今すぐ着陸して行動終了（自動着地待ちより1ターン早く攻撃に移れる）。
  残り1ターンなら空中のまま待機（プレイヤーフェイズを近接無敵で過ごし、
  次フェイズ開始にタダで自動着地→即行動）。着陸マスは 地上の道のり→地形防御→移動の少なさ で選ぶ。
  歩けない地形（城壁・山など）へは手動着陸しない（飛翔切れの自動着地で乗るのは従来どおり合法）
- 同一行動での離陸→着陸は無し（プレイヤーと同ルール）。着陸の行動終了は
  TurnManager の TakeAction 後 SetActed(true) で満たされる（FinishAction は通さない）
- 離陸からの攻撃も挑発として数える（TakeOff かつ target ありは Attack と同じ扱い。Phase 17 の延長）
- アセット: Stage_01(3,9)・Stage_Test(5,8) の敵飛行兵 initialFlightTurns 99→0（暫定対応を撤去）
- 検証用ステージ: Stage_AITest_Flight（10×10・y=6 に城壁列で地上の抜け道は x=0 のみ・
  南に城壁囲いの中庭※入口(5,0)のみ。ダミー(5,1)・味方弓兵(3,2) vs 敵飛行兵(8,9)地上開始。
  離陸「空2ターン+着陸1 vs 陸4ターン」→城壁越え→(5,0)へ着陸→翌ターン攻撃が見られる。
  弓兵で入口を塞ぐと別マスへ着陸先を付け替える）

### 集中攻撃の合意仕様（Phase 21・2026-07-18 作者合意）

- 敵フェイズの開始に1回だけ、EnemyAI.BeginEnemyPhase が「皆で掛かれば倒し切れる相手」を
  **1体だけ**集中攻撃の目標（focusTarget）に選ぶ（TurnManager が敵の行動ループの前に呼ぶ）
- 火力見込みの数え方: プレイヤー1人ごとに、参加できる敵の
  「後衛武器の単発ダメージの合計 ＋ 近接ダメージの上位k件」
  （k = そのプレイヤーの隣接マスのうち盤内かつ「空き or 敵占有」の数＝近接が取り付ける口の数）。
  参加できる敵 = 未行動 かつ（突撃型 or 挑発済み）かつ CanEngage を通る敵のみ
- 目標の条件: 火力見込み ≥ 現在HP（今フェイズ中に倒し切れる見込みがある相手だけ狙う）。
  候補が複数なら最小HP優先（同点は名簿順）。いなければ「目標なし」で従来どおりの各自判断
- FocusBonus +200: 目標への攻撃に加点。撃破ボーナス 1000 より下（目の前の確実な撃破は
  捨てない）・ガード役潰し 50 より上（倒せる相手がいないときは皆で同じ相手を削る）。
  離陸からの空中攻撃にも効く（TryPlanTakeOff が攻撃探索 TryFindBestAttack を共用しているため）
- 目標が倒れたら、次の敵の思考開始時に**再選定**する（focusAssigned フラグで管理 —
  倒れたユニットは Destroy で参照が null になるため「元から目標なし」との区別に必要）
- static な状態（focusTarget など）は RuntimeInitializeOnLoadMethod でリセット
  （UnitRegistry と同じパターン。エディタで再生を繰り返しても前回の目標が残らない）
- 思考ログ: フェイズ開始に「AI方針: 集中攻撃の目標 = …（HP・火力見込み）」、
  各行動の理由に「・集中攻撃」を表記
- 検証用ステージ: Stage_AITest_Focus（10×10 全平地・弱ダミーHP12 が (4,4)(8,6)・
  通常ダミー(2,4)・敵斧兵4体。ターン1で「弱ダミーへ収束→撃破→もう1体へ再選定」、
  ターン2で「目標なし」のログが見える）

### デバッグ/テスト用UI（2026-07-18 作者依頼。統合調整の補助として追加）

- ① ユニット情報パネル（UI/DebugUnitPanel・画面右上）: 盤上のユニット（敵味方どちらでも）を
  クリックすると能力値・武器・状態を表示。空きマスのクリックで閉じる・盤外クリックでは保持・
  敵フェイズ中やメニュー表示中でも確認できる・ゲームの状態は一切変えない。
  邪魔なときは Inspector の panelEnabled で OFF にできる
- ② 戦闘予測の改良: 対象選択に入った瞬間に即表示（従来のマウスオーバー待ちを廃止）＋
  三すくみ補正の専用行（補正 ±0 のときも「三すくみ ±0（補正なし）」と必ず表示）
- ③ 範囲表示: 自軍ユニットを選ぶと移動範囲（水色）の外周に「攻撃だけ届くマス」を赤系で
  重ね表示（BattleController.ComputeAttackCells。壁マスは除外・救出/引き受け後は非表示）。
  Idle 中に敵をクリックするとその敵の移動+攻撃範囲プレビュー（ShowEnemyPreview。
  空きマスクリック/ESC/右クリック/自軍選択/敵フェイズ開始で解除）。
  色の注記: スクリプト既定の attackRangeColor は赤 (1, 0.5, 0.5) だが、シーンの直列化値は
  オレンジ (1, 0.7, 0.5) のままで作者テスト済み・承認済み
  （変える場合は GridManager の Inspector で色を変えて Ctrl+S 保存→シーンをコミット）
- ④ 被ダメ確認コマンド: 行動メニューに攻撃と同じ条件（攻撃対象がいるとき）で並ぶ。
  選ぶと閲覧専用の State.DamageCheck に入り、BattleForecast.ShowIncoming が
  「その位置で逆に攻撃されたら」の予測を表示（受けるダメージの内訳・三すくみは
  「相手が有利/不利」の表現・自分の残りHP見込みと「倒される！」警告・
  「単発のみ・挟撃含まず」の注記）。クリックは無視され、ESC/右クリックでメニューへ戻る。
  行動は消費しない

### 兵種データの合意仕様（2026-07-18 作者合意）

- 兵種は ClassData（ScriptableObject、Assets/_Game/Data/Classes/）で38種を定義:
  下級14職・上級24職。聖騎馬兵は槍兵と騎馬兵の**共有**昇格先でアセットは1つ。
  各兵種 = 名前・階級（下級/上級 ClassTier）・移動タイプ（UnitClass）・移動力・装備可能武器種・
  特攻タグ（ClassTag: 騎馬/天馬/竜）・昇格先（下級のみ最大2つ）。兵種表は作者提供（2026-07-18）
- 特攻タグと昇格先は**データのみ**。クラスチェンジの実行（レベル/経験値が前提）と
  特攻ダメージの戦闘反映（特効武器が前提）は将来実装
- 昇格で騎乗になる職（聖騎馬兵・鎧騎馬兵・ノマディ・ヴァルキュリア・ダークナイト）は移動タイプ「騎兵」
  → 騎乗の地形ルール・救出可（容量1）・騎馬特攻タグが付く
- 魔法＝魔導＝魔導書は同一武器（WeaponType.Magic）。光魔法は司祭用の別武器種（定義のみ）。
  杖は攻撃できない武器（回復などの効果は将来）
- 武器制限は**警告のみ**（UnitData.OnValidate と Unit.Initialize で Console 警告。動作は止めない）
- 既存ユニットへの適用（作者合意）: 剣歩兵（味方兵・ダミー2体）は剣闘士（移動3維持。剣士だと移動4のため）。
  武器を兵種に合わせて変更 = 味方飛行兵 剣→槍（天馬兵）、斧騎兵2体 斧→槍（騎馬兵。
  名前も味方槍騎兵/敵槍騎兵へ変更。ファイル名 Unit_*_AxeCavalry は GUID 保全のため旧名のまま）。
  敵飛行兵は竜騎兵になり移動力 6→5（ステージ上の敵の到達が変わりうる点は作者了承）

### 所持品（インベントリ）の合意仕様（Phase 22〜24・2026-07-19 作者合意）

- 所持品は UnitData.items（List<ItemData>・上限7 = 定数 InventoryCapacity）。一番上の武器が初期装備。
  items が空なら旧 weapon 欄1つを持っている扱い（新旧共存フォールバック。GetInitialItems / GetInitialWeapon）
- 種類は武器（WeaponData）と道具（ToolData）の2つ。どちらも抽象基底 ItemData（DisplayName のみ・直列化フィールド無し）の子。
  WeaponData の基底を ItemData に差し替えてもクラス名・スクリプトGUIDは不変なので既存アセットは無傷。
  素材（第3の種類）・武器耐久は仕様書どおり見送り（設計だけ拡張に耐える形にしておく）
- ランタイムは Unit.Items（ItemSlot＝item + 残り回数 UsesLeft）で持ち、装備中の武器は Unit.EquippedWeapon。
  **Unit.Weapon => EquippedWeapon の窓口差し替え**で戦闘・AI・UI が自動追随（全参照が Weapon 窓口経由・Data 直読み禁止が効く）
- 装備の持ち替え・武装解除は**行動を消費しない**（合意(a)。同一行動で持ち替え→攻撃可。移動取り消しでも装備は戻らない）。
  「所持品」コマンドから ItemListMenu で選ぶ。BattleController に State.ItemSelect（9状態目）。
  行は 武器（装備可＝押せる／装備中・装備不可＝無効行）・道具（残り回数付き・HP満タンは無効行）・
  最下行「武装解除」（武器装備時のみ）。実行後はメニューを開き直す（攻撃対象などが新装備で再計算）。
  救出・引き受けの制限分岐では「所持品」を出さない（早期 return で自動除外＝既存の制限を維持）
- 道具（傷薬 Tool_Vulnerary・HP10回復×3）は**使うと行動終了**（合意(b)。装備変更と違い行動を消費）。
  HP満タンには使えない（無効行）。効果は使用者自身のHP回復（Unit.UseTool → Unit.Heal で最大HPクランプ・
  残り回数-1・0で所持品から消える）。Heal は杖の回復とも共通の土台
- 敵AIは当面所持品を使わない（初期装備固定・道具なし。「敵は救出系を使わない」と同じ前例。
  Unit.Weapon 窓口で戦闘・AIは自動追随するのでコード変更なし）

### 杖（ライブ）の合意仕様（Phase 25・2026-07-19 作者合意）

- 杖は攻撃できない後衛武器。担い手は新ユニット**味方修道士**（兵種 Class_Monk＝歩兵・移動3・杖のみ。
  現行17体は杖を装備できない）。修道士の所持品は [ライブ, 傷薬]、初期装備＝ライブ
- 「杖」コマンド: 杖を装備し、回復できる相手が射程内にいるとき、攻撃と同じ位置に出る（杖ユニットは
  攻撃できない＝攻撃コマンドとは排他）。回復対象を緑で選び、クリックで回復して**行動終了**（合意(b)）
- 回復量 = **使用者の魔力 + 杖の威力**（合意(c)。ライブは威力10・射程1〜1。修道士 魔力6＋10＝16）。
  最大HPは超えない（Unit.Heal でクランプ）。回復対象＝同陣営・生存・HP満タン未満・射程内
- 範囲の緑表示: 選択時に移動範囲（水色）の外側へ「杖が届くマス」を緑フリンジで表示
  （HighlightKind.HealRange。攻撃フリンジとは排他。BattleController.ComputeHealCells）。回復対象の選択ハイライトも緑。
  杖の射程は攻撃の後衛武器と違い、移動しても縮まない（杖は攻撃ではないため）
- 可否・回復量の判定は Combat/StaffRules.cs に一元化（CombatRules / RescueRules と同じ方針。
  HasStaffEquipped / TryGetStaffRange / FindHealTargets / HealAmount）。対象選択は既存 State.UnitTargetSelect に
  purpose Heal を追加して再利用（ハイライト色を引数化＝救出系は赤、杖は緑）
- 飛翔中の味方への杖は可（杖は後衛武器＝地上からでも空中の味方に届く。使用者が飛翔中なら空中の味方のみ
  ＝CanEngage のミラー。StaffRules.CanReach。現状の担い手は歩兵なので使用者飛翔は起きないが将来に備え実装）
- **対空判定から杖を除外**: CombatRules.IsAntiAirCapable(unit)＝「後衛の攻撃武器（弓・魔導書・光魔法）」を
  新設し、対空を見る3箇所（戦闘可否 CanEngage・すり抜け MovementCalculator・対空圏 ThreatMap.GetAirThreatCells）を統一。
  杖は攻撃できないので対空でない → 修道士は敵飛行兵の頭上を塞がず、AIの対空圏回避の対象にもならない
- 敵からは「武装無し扱い」で不利に殴られる（既存の三すくみルールが Weapon=null 相当で自動成立）
- アセット: W_Live（ライブ）・Unit_Player_Monk（味方修道士 HP17/力1/魔力6/技5/速さ6/守備2/魔防6・初期案）。
  Stage_01 の (7,0) に修道士を配置（味方11体目・y=0 は平地の空きマス）

### Phase 8 以降の追加合意（2026-07-08）

- 武器分類: 剣・斧・槍＝前衛武器、弓・魔法＝後衛武器（WeaponData にカテゴリ欄を追加、データ駆動）
- 新しい三すくみ補正（Phase 9 で移行）: 有利側が攻撃 = +max(2, 攻撃側の技 − 防御側の速さ)、
  有利側が防御 = −max(2, 防御側の速さ − 攻撃側の技)。旧 ±1 固定は廃止
- 敵AIは新戦闘ルールに追随させる。救出・飛翔コマンドは当面敵は使わない
- メニューUIは IMGUI で簡易実装（uGUI 化は素材差し替え時期にまとめて）
- 必中（RNGなし）・反撃なし（CanCounter フック維持）・ScriptableObject データ駆動は継続
- 育成要素（レベル・経験値・成長率）はロードマップ外（将来枠）

## コード構成（Assets/_Game/Scripts/）

- Core/ … BattleController(操作の司令塔・9状態FSM: Idle/MoveSelect/CommandMenu/TargetSelect/UnitTargetSelect/TileSelect/CargoSelect/DamageCheck/ItemSelect。
  飛翔・着陸コマンドと飛翔の段階取り消しCancelFlightAndReturn・攻撃可能マス表示ComputeAttackCells・
  所持品コマンド（装備/武装解除/道具使用）・杖コマンド（回復対象選択→ExecuteHeal・杖が届くマス表示ComputeHealCells）・
  敵の行動範囲プレビューShowEnemyPreview・被ダメ確認の閲覧状態もここ),
  ActionContext(1行動の文脈・移動取り消し・再移動予算・取り消し不能点Commit・飛翔の中間点MarkFlight),
  TurnManager(フェイズ・勝敗・ターン制限判定・簡易UI・フェイズ開始時の飛翔Tick・
  敵AI思考ログの切替enemyAILog・敵フェイズ内の敵HasActed管理※フェイズ終了時に戻す),
  BattleSetup(StageDataを読んで初期配置・開始時飛翔の適用・配置マスの兵種チェック)
- Grid/ … GridManager(盤面・可変サイズApplyStageSize・カメラ自動フィットFitCamera・座標変換・
  多層ハイライト・地形適用ApplyTerrain・地形情報の画面下表示※騎乗差分も表示),
  TileData(1マスの論理データ。IsWalkable/CanFlyOver/MoveCost/DefenseBonus に加え
  兵種別の IsWalkableFor/MoveCostFor),
  TerrainType(地形enum+TerrainDef定義。城壁Rampart・飛行可否canFlyOver・騎乗の mountedWalkable/mountedCost),
  MovementCalculator(ダイクストラ移動範囲・味方すり抜け・兵種別コスト/通行制限・飛翔中はコスト1で対空敵※CombatRules.IsAntiAirCapableのみすり抜け不可・
  通行判定はCanTraverseに集約・目標マス群への距離マップGetDistanceMap※敵AIの接近用)
- UI/ … ActionMenu(IMGUI行動メニュー。Entryリスト駆動・BattleControllerが自動生成),
  CargoListMenu(貨物リスト選択。名前+HP表示・BattleControllerが自動生成),
  ItemListMenu(所持品リスト。ActionMenu 型・enabled フラグで押せない行を灰色無効化・BattleControllerが自動生成),
  BattleForecast(戦闘予測パネル・右下。対象選択に入った瞬間に即表示・三すくみ専用行・
  被ダメ確認の逆向き予測ShowIncomingもここ・BattleControllerが自動生成),
  DebugUnitPanel(デバッグ用ユニット情報パネル・右上。クリックしたユニットの能力値・武器・状態を表示。
  panelEnabledでOFF可・ゲーム状態は変えない)
- Units/ … Unit(盤上のユニット・ランタイム能力値・行動済み・格納Carried/IsCarried・飛翔IsFlying/FlightTurnsLeft・
  AI性格AIProfile/挑発済みIsProvoked※永続・所持品Items/装備EquippedWeapon※Weapon窓口・Equip/Unequip・
  回復Heal※最大HPクランプ・道具使用UseTool),
  ItemSlot(所持品1つのランタイム状態: item + 残り回数UsesLeft),
  UnitClass(移動タイプenum+IsMounted/IsCavalry/DisplayName),
  ClassTier(階級enum: 下級/上級), ClassTag(特攻タグ[Flags]: 騎馬/天馬/竜),
  RescueRules(救出系コマンドの可否判定一元化・乗り込む判定・飛翔の制限と空中引き受け・
  貨物の兵種で判定する GetDroppableCargoes/GetDropCells・死亡時配置先探索FindReleaseCell),
  UnitRegistry(全ユニットの名簿・輸送隊死亡フラグPlayerTransporterLost), Faction(陣営enum)
- Combat/ … CombatSystem(攻撃解決・挟撃・ガード・反撃フック・PredictTotalDamage),
  CombatRules(射程・三すくみ・挟撃可否・飛翔の戦闘制限CanEngageの一元判定・対空判定IsAntiAirCapable※杖除外・Phase 25・
  攻撃できる立ち位置一覧GetAttackFromCells・挟撃の反対側マスGetPincerOppositeCell・
  仮定位置ガード判定WouldGuardAt・ガード役かどうかIsGuardingSomeone※Phase 18),
  StaffRules(杖の可否・回復対象・回復量の一元判定※Phase 25。HasStaffEquipped/TryGetStaffRange/FindHealTargets/HealAmount),
  DamageCalculator(物理/魔法分岐・DamageBreakdown内訳・飛翔中は地形防御なし),
  WeaponType(7種+魔法ダメージ判定IsMagicDamage), WeaponCategory(前衛/後衛)
- Data/ … UnitData(7能力値+兵種データclassData+所持品items※上限InventoryCapacity=7。移動タイプ/移動力は
  EffectiveClass/EffectiveMove、初期所持品/装備は GetInitialItems/GetInitialWeapon で解決・武器不一致はOnValidateで警告),
  ClassData(兵種1種の定義: 名前・階級・移動タイプ・移動力・装備可能武器・特攻タグ・昇格先),
  ItemData(所持品の抽象基底・DisplayName のみ), WeaponData(: ItemData), ToolData(: ItemData・道具。healAmount/maxUses),
  StageData(初期配置+開始時飛翔initialFlightTurns+
  AI性格aiProfile+地形マップterrainRows+盤面サイズgridWidth/gridHeight+ターン制限turnLimit),
  TerrainTable(地形定義一覧・記号引き) (ScriptableObject定義)
- AI/ … EnemyAI(点数評価型・「考えるEvaluate→動かすExecute」の2段構え・
  思考の順番は 攻撃→待ち伏せゲート→ガード→飛翔(空中なら着陸/巡航、地上の飛行兵は離陸検討)→接近※Phase 20・
  接近は距離マップの3段構え(経路→敵無視→直線距離)・日本語思考ログ・同点は地形防御の高いマス優先・
  性格ゲート※待ち伏せ型は挑発まで動かない＝挑発判定はTakeAction冒頭(被弾)と攻撃プラン成立時※離陸攻撃も含む・
  挟撃のお膳立て+2とガード役潰し+50※Phase 18・守り配置TryPlanGuard※Phase 19・
  飛翔probe評価ComputeGroundInfo/TryPlanTakeOff・巡航TryPickCruiseCell・着陸TryPlanLanding※Phase 20・
  集中攻撃※Phase 21=敵フェイズ開始のBeginEnemyPhaseで目標focusTargetを1体選定・倒れたら再選定・
  staticはRuntimeInitializeOnLoadMethodでリセット。
  点数定数は先頭の1ブロックに集約: KillBonus 1000／FocusBonus 200／GuardBreakKillBonus 50／
  PincerSetupBonus 2／AirThreatPenalty 1),
  ThreatMap(盤面の危険の読み取り。挟撃されそうな味方と防げる最大ダメージの一覧
  FindPincerThreatenedAllies※self は盤面に居ない扱いで判定。Phase 19、
  対空射程マスの集合GetAirThreatCells※Phase 20),
  EnemyAIProfile(性格enum: 突撃型/待ち伏せ型。Phase 17),
  ActionPlan(行動予定の構造体+ActionKind。Guard※Phase 19、
  TakeOff/LandAndWait※Phase 20 まで全種実装済み)
- Editor/ … UnitDataEditor(兵種データ設定時は旧 兵種/移動力 欄を隠し「データ由来」を読み取り専用表示),
  WeaponDataEditor, ToolDataEditor, ClassDataEditor(Inspectorの日本語表示。エディタ専用・ビルド非含有)

アセット実体: Assets/_Game/Data/
（Unit_Player, Unit_Enemy, Unit_Player_Archer, Unit_Player_Mage, Unit_Player_Cavalry,
  Unit_Player_Transport, Unit_Player_Flier, Unit_Player_AxeFighter, Unit_Player_AxeCavalry,
  Unit_Enemy_Lancer, Unit_Enemy_Archer, Unit_Enemy_Flier, Unit_Enemy_Mage, Unit_Enemy_LanceFlier,
  Unit_Enemy_AxeCavalry, Unit_Test_Dummy, Unit_Test_Dummy_Weak（弱ダミーHP12・集中攻撃の検証用）,
  Unit_Player_Monk（味方修道士・杖ユニット。Phase 25）,
  W_Sword, W_Axe, W_Lance, W_Bow, W_Tome, W_Live（杖・ライブ。Phase 25）,
  Tool_Vulnerary（傷薬。道具・Phase 24）, Stage_Test, Stage_01, Stage_AITest_Path,
  Stage_AITest_Ambush, Stage_AITest_Pincer, Stage_AITest_Guard, Stage_AITest_Flight,
  Stage_AITest_Focus, TerrainTable。
  Classes/ サブフォルダに兵種 ClassData 38種: Class_Tactician（軍師）〜Class_MaligKnight（魔竜騎兵）。
  ※Unit_Player_AxeCavalry / Unit_Enemy_AxeCavalry はファイル名こそ旧名だが中身は槍騎兵（兵種データ導入時に持ち替え））
シーン: Assets/_Game/Scenes/BattleScene.unity

## 次にやること

敵AI強化ロードマップ（Phase 16〜21）は**完走**（Phase 16: 271755e・17: f4c4b61・18: bceef88・
19: 6746206・20: 4c3f415・21: ca9acaa）。統合調整も完了（2026-07-18 作者確認:
手応え・面白さ十分で調整不要。デバッグ/テストUIの追加も①〜④で打ち止め）。

その後、兵種データ基盤（2026-07-18〜19。「兵種データの合意仕様」の節）を導入し、
**所持品（インベントリ）ロードマップ（Phase 22〜25）も完走**（2026-07-19）。

**現在アクティブなロードマップは無い。次は作者と次期ロードマップの優先度を相談してから着手する**
（下の「持ち越し候補」から作者が選ぶ。フェーズ着手は明示の合図をもらってから）。

### 所持品（インベントリ）ロードマップ（Phase 22〜25・2026-07-19 キックオフ〜完走）

確定仕様は「所持品の合意仕様」「杖（ライブ）の合意仕様」の節が正。以下はキックオフ時の
合意・設計判断の記録（参照用）。

- 背景・方針: 作者の仕様書 `所持品管理仕様書.md` に基づくインベントリシステム。方針は
  「今のマップ（Stage_01）のプレイだけで確認できる機能」。所持品は特効武器・武器耐久・交換・
  杖効果・ショップすべての土台で、兵種の「装備可能武器」データが初めて本来の役割を果たす。
  優先度は最優先（拠点到達・育成は他機能の前提でないため後回しで損なし。2026-07-19 作者合意）
- キックオフ合意（2026-07-19 ダイアログ・全て推奨案）:
  - (a) 装備変更・武装解除は行動を消費しない（FE準拠。同一行動で持ち替え→攻撃可。移動取り消しでも装備は元に戻らない）
  - (b) 道具・杖は使うと行動終了。HP満タンの対象（傷薬は自分）には使えない
  - (c) ライブ（杖）の回復量 = 使用者の魔力 + 杖の威力
  - (d) 杖の担い手 = 新ユニット「味方修道士」（現行17体の兵種は全て杖を装備できないため）
- プラン内で決めた既定（作者はプラン確認時に否認可能）:
  - 敵AIは当面所持品を使わない（装備固定・道具なし。「敵は救出系を使わない」と同じ前例）
  - 飛翔中の味方への杖は可（杖は後衛武器＝対空と同じ「空に届く」考え方。自分が飛翔中なら空中の味方のみ＝CanEngage のミラー）
  - 対空判定から杖を除外（杖は攻撃できないのに、現行の「後衛武器＝対空」判定だと修道士が
    敵飛行兵の通行を塞ぎ・AIの対空圏回避の対象になってしまうため）
  - 武装解除は所持品リストの最下行に置く（行動メニューの肥大を避ける）
  - 仕様書どおり見送り: 素材（第3の種類）・武器耐久・敵の武装解除スキル（設計だけ拡張に耐える形にしておく）
- 設計の骨子:
  - データモデル: 抽象基底 ItemData（DisplayName 抽象プロパティのみ）を新設し、WeaponData : ItemData に
    基底差し替え（クラス名・スクリプトGUID不変なので既存アセット・参照は無傷）。道具は
    ToolData : ItemData（toolName / healAmount=10 / maxUses=3。効果種別 enum は2種類目の道具が来たら追加）
  - UnitData: List<ItemData> items を追加（上限7 = 定数 InventoryCapacity・OnValidate 警告のみ）。
    既存 weapon 欄は旧式フォールバックとして残す（items が空なら [weapon] 相当。unitClass/move と同じ新旧共存）
  - Unit ランタイム: ItemSlot（item + usesLeft の素の C# クラス）を Initialize で構築し EquippedWeapon を新設。
    Unit.Weapon => EquippedWeapon に差し替えれば戦闘・AI・UI が自動追随（全参照が窓口経由・Data 直読み禁止が効く）
  - UI: ItemListMenu を CargoListMenu の型で新設（装備不可・装備中・満タンの道具は無効行で表示）。
    BattleController に State.ItemSelect を追加（9状態目）。キャンセルは右クリック/ESC で1段階ずつ
  - 杖: 可否判定は Combat/StaffRules.cs に一元化（CombatRules/RescueRules と同じ方針）。対象選択は既存
    UnitTargetSelect に purpose Heal を追加して再利用。範囲の緑表示は HighlightKind.HealRange（新ビット・AttackRange の下）
  - 敵AI: コード変更なし（敵は初期装備のまま・道具なし。Unit.Weapon 窓口で自動追随）
- 各フェーズのコミット（feat スクリプト + data アセットの小刻みコミット。全て Play 確認済み）:
  - フェーズ22 データ基盤（挙動不変）: 3293343（+ Class_MasterThief.asset 再直列化 chore 7c7331b）
  - フェーズ23 装備コマンド: feat d8f5d04 / data cb6effb
  - フェーズ24 道具・傷薬: feat f689ddf / data 144a598
  - フェーズ25 杖「ライブ」: feat 00a1411 / data 1f35a2a
- プラン全文: `/home/wawo/.claude/plans/tsumetsume-strategy-glimmering-horizon.md`（WSL側）。
  作者の編集用コピー: `C:\Users\wawo\Desktop\所持品ロードマップ（フェーズ22〜25）実装プラン.md`

### 持ち越し候補（次期ロードマップの相談用。優先度は作者が決める）

過去の合意で先送りになった項目のメモ:
- 特効ダメージ＋特効武器（所持品の持ち替えが活きる。兵種の特攻タグ〔騎馬/天馬/竜〕が前提として導入済み）
- 交換・輸送隊への預かり（所持品システムの自然な延長）
- 武器耐久（作者検討中。残り回数の仕組みは道具の ItemSlot.UsesLeft で先行導入済み）
- 勝利条件バリエーション（拠点到達など。Phase 15 と敵AI強化キックオフで2回先送り）
- 救出系コマンドの敵AI使用（現状は「敵は救出系を使わない」合意のまま）
- 敵の性格の追加（挑発後も動かない完全固定砲台型など。Phase 17 で先送り）
- 敵側の輸送隊・敵の装備切替/道具使用（現状はプレイヤー専用・敵は初期装備固定）
- uGUI化・本番素材への差し替え（IMGUIと色付き四角の置き換え。素材差し替え時期にまとめて）
- 育成要素（レベル・経験値・成長率。従来からロードマップ外の将来枠）
- 兵種データの残り（クラスチェンジの実行・特攻ダメージの戦闘反映・光魔法の武器アセットと効果・
  主人公＝軍師ユニットの作成。データ基盤は導入済み）
- 新ステージの追加・能力値や配置の調整（アセット編集）は作者の依頼があれば随時

## 環境・運用の注意

- Unity は Windows 側で作者が操作。Claude は WSL からファイル編集と git のみ。
  Play モード確認は作者に依頼する（手順を番号付きで丁寧に案内する）
- シーン変更後は作者に Ctrl+S 保存を依頼してから git の状態を確認する
- 新 Input System のみ有効（activeInputHandler=1）。クリックは Mouse.current を使う
- 各フェーズ完了ごとに動作確認→コミットの小刻み進行。コミットメッセージは日本語
- 選択式ダイアログの回答は履歴で見つけにくい。回答をもらったら次の返信で必ず復唱し、
  フェーズ着手などの大きな合図は明示の指示をもらってから動く（2026-07-17 の作者フィードバック）
- Assets/_Recovery/ は Unity の自動生成物で .gitignore 済み
- 新規アセット追加時は Claude が .asset と .meta（GUID自動生成）をペアで作成する。
  .asset の YAML 編集は Unity を閉じた状態で行うのが安全（作者に事前確認する）
- 作者PCは Windows 11 の「スマートアプリコントロール」有効のため Burst の DLL がブロックされる
  （error code 4551）。ゲームコードは Burst 不使用なので Jobs > Burst > Enable Compilation を
  オフにして回避済み（2026-07-16。PCごとの設定で git には入らない）
